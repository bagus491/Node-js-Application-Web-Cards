ini adalah aplikasi login logout lalu dengan multer ===>soaxdo.Galerry Cards

===> nodeJs application Web

===> Package ==> core module => fs

	 ==> npm/third party => express
		 => ejs
		 => express-ejs-layouts
		 => express-validator@6.1.0
		 => express-session
		 => connect-flash
		 => cookie-parser
		 => body-parser
		 => multer
		 => jsonwebtoken
		 => bcrypt
		 => nodemon


==> midleware => url.encoded
		 path
		 express.static

flow ==> Users must register if didnt have Account
	 Users can login if already have account
	 if users from login , cards will show with image 
	if register must uploads image for image cards

===> ---- must include ==> mongodb
			   and mongoose 

author => baguskalam

thansk you